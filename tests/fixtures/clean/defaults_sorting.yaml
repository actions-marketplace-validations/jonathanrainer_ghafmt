name: Defaults Sorting

on: push

defaults:
  run:
    shell: bash
    working-directory: ./app

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: pwsh
        working-directory: ./src
    steps:
      - run: echo "build"
