name: Job Secrets Sorting

on: push

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    secrets:
      AWS_KEY: ${{ secrets.AWS_KEY }}
      DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
