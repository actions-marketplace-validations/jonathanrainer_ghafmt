name: Minimal Workflow
on: push
jobs:
  single-step:
    runs-on: ubuntu-latest
    steps:
      - run: echo "only step"
  with-checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
